const camion = require('./camion.models');
const paquete = require('./paquete.models');
const provincia = require('./provincia.models');
const camionero = require('./camionero.models');
const entrega = require('./entrega.models')

module.exports = {
    camion,
    paquete,
    provincia,
    camionero,
    entrega
}